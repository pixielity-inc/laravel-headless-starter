# ==============================================================================
# LOCAL DEVELOPMENT ENVIRONMENT
# ==============================================================================
# This file contains environment variables for local development.
# Copy this file to the root directory as .env to get started.
#
# Usage:
#   cp env/.env.local .env
#   php artisan key:generate
#   composer dev
# ==============================================================================

# ------------------------------------------------------------------------------
# Application Configuration
# ------------------------------------------------------------------------------
# Core application settings that define the app's identity and behavior.
# These values control fundamental aspects of your Laravel application.
# ------------------------------------------------------------------------------

APP_NAME="Laravel Headless Starter"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000

# Localization settings
APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

# Maintenance mode configuration
APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# ------------------------------------------------------------------------------
# Laravel Octane Configuration
# ------------------------------------------------------------------------------
# High-performance server settings using FrankenPHP driver.
# Octane keeps your application in memory for blazing-fast response times.
#
# OCTANE_WORKERS: Number of workers (auto = CPU cores, or set specific number)
# OCTANE_MAX_REQUESTS: Restart worker after N requests (prevents memory leaks)
# OCTANE_WATCH: Auto-reload on file changes (development only)
# ------------------------------------------------------------------------------

OCTANE_SERVER=frankenphp
OCTANE_HTTPS=false
OCTANE_WORKERS=auto
OCTANE_MAX_REQUESTS=500
OCTANE_WATCH=true
FRANKENPHP_CONFIG=""

# ------------------------------------------------------------------------------
# Security Configuration
# ------------------------------------------------------------------------------
# Password hashing and encryption settings.
#
# BCRYPT_ROUNDS: Higher = more secure but slower (10-12 recommended)
# ------------------------------------------------------------------------------

BCRYPT_ROUNDS=12

# ------------------------------------------------------------------------------
# Logging Configuration
# ------------------------------------------------------------------------------
# Configure how application logs are handled and stored.
#
# LOG_CHANNEL: Main logging channel (stack, single, daily, slack, etc.)
# LOG_LEVEL: Minimum level to log (debug, info, notice, warning, error, critical)
# ------------------------------------------------------------------------------

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# ------------------------------------------------------------------------------
# Database Configuration
# ------------------------------------------------------------------------------
# SQLite is used by default for simplicity in local development.
# For production, consider MySQL or PostgreSQL for better performance.
#
# Supported drivers: sqlite, mysql, pgsql, sqlsrv
# ------------------------------------------------------------------------------

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

# ------------------------------------------------------------------------------
# Session Configuration
# ------------------------------------------------------------------------------
# Session storage and cookie settings for maintaining user state.
#
# SESSION_DRIVER: Where sessions are stored (file, cookie, database, redis)
# SESSION_LIFETIME: Session timeout in minutes
# ------------------------------------------------------------------------------

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# ------------------------------------------------------------------------------
# Cache Configuration
# ------------------------------------------------------------------------------
# Cache driver and settings for improved application performance.
#
# Supported drivers: file, database, redis, memcached, dynamodb, octane
# For production, use Redis or Memcached for better performance.
# ------------------------------------------------------------------------------

CACHE_STORE=database
# CACHE_PREFIX=

# ------------------------------------------------------------------------------
# Queue Configuration
# ------------------------------------------------------------------------------
# Background job processing settings for asynchronous tasks.
#
# Supported drivers: sync, database, redis, sqs, beanstalkd
# Use 'sync' for local development, 'redis' or 'database' for production.
# ------------------------------------------------------------------------------

QUEUE_CONNECTION=database

# ------------------------------------------------------------------------------
# Redis Configuration
# ------------------------------------------------------------------------------
# Redis settings for caching, sessions, and queues (optional but recommended).
#
# Redis provides high-performance in-memory data storage.
# Install Redis: brew install redis (macOS) or apt-get install redis (Linux)
# ------------------------------------------------------------------------------

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# ------------------------------------------------------------------------------
# Mail Configuration
# ------------------------------------------------------------------------------
# Email sending configuration for notifications and transactional emails.
#
# MAIL_MAILER: Driver to use (smtp, sendmail, mailgun, ses, postmark, log)
# In local development, 'log' writes emails to storage/logs/laravel.log
# ------------------------------------------------------------------------------

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# ------------------------------------------------------------------------------
# Broadcasting Configuration
# ------------------------------------------------------------------------------
# Real-time event broadcasting settings for WebSockets and push notifications.
#
# Supported drivers: log, pusher, ably, redis, reverb, null
# Laravel Reverb is the official WebSocket server for Laravel applications.
# ------------------------------------------------------------------------------

BROADCAST_CONNECTION=log

# Laravel Reverb Configuration
REVERB_APP_ID=app-id
REVERB_APP_KEY=app-key
REVERB_APP_SECRET=app-secret
REVERB_HOST=localhost
REVERB_PORT=8080
REVERB_SCHEME=http

# Pusher Configuration (if using Pusher instead of Reverb)
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# ------------------------------------------------------------------------------
# Filesystem Configuration
# ------------------------------------------------------------------------------
# File storage driver configuration for uploads and file management.
#
# Supported drivers: local, public, s3, ftp, sftp
# ------------------------------------------------------------------------------

FILESYSTEM_DISK=local

# ------------------------------------------------------------------------------
# AWS Configuration
# ------------------------------------------------------------------------------
# Amazon Web Services credentials and settings for S3, SES, SQS, etc.
#
# Required only if using AWS services (S3 storage, SES email, SQS queues).
# Keep these credentials secure and never commit them to version control.
# ------------------------------------------------------------------------------

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# ------------------------------------------------------------------------------
# Frontend Configuration
# ------------------------------------------------------------------------------
# Variables exposed to frontend build tools (Vite, Webpack, etc.).
#
# Note: Only variables prefixed with VITE_ are accessible in frontend code.
# ------------------------------------------------------------------------------

VITE_APP_NAME="${APP_NAME}"
